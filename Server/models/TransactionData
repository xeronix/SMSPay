var mongoose = require("mongoose");

var transactionDataSchema = new mongoose.Schema({
	id: String,
	from: String,
	to: String,
	amount: Number
});

module.exports = mongoose.model("transactiondata", transactionDataSchema, "transactiondata");
